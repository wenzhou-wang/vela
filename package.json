{
  "name": "vela",
  "version": "0.1.0",
  "description": "A WebGPU-first 3D rendering engine for the modern web",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "docs:llms": "node scripts/generate-llms.mjs"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.40",
    "typescript": "^5.5.0",
    "vite": "^5.3.0",
    "wgsl_reflect": "^1.3.0"
  }
}
